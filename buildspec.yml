# buildspec.yml
version: 0.2

phases:
  install:
       install:
        runtime-versions:
         java: corretto11
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/psql_test.jar
